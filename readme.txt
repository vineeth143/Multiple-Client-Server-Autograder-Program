++this code is compiled and tested in wsl

++compilation instructions

--gcc server.c -o server
--gcc client.c -o client

++./server 8080
++./client localhost 8080

--i have used gets() for reading a string hence it gives a warning message at the time of compilation
